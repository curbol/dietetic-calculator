{
  "name": "my-nexus-app-30065397133065463",
  "license": "UNLICENSED",
  "scripts": {
    "format": "npx prettier --write './**/*.{ts,md}'",
    "dev": "nexus dev",
    "build": "nexus build",
    "start": "node .nexus/build/api",
    "migrate-save": "npx prisma migrate save --experimental",
    "migrate-up": "npx prisma migrate up --experimental",
    "seed": "ts-node prisma/seed"
  },
  "prettier": {
    "semi": false,
    "singleQuote": true,
    "trailingComma": "all"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "nexus": "^0.22.0",
    "nexus-plugin-prisma": "^0.8.0"
  },
  "devDependencies": {
    "@types/cors": "^2.8.6",
    "prettier": "^2.0.5"
  }
}
